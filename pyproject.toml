[project]
name = "formula-buggy"
version = "0.1.0"
description = "Reproducible Blender asset pipeline for Formula Buggy"
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = [
    "bpy==5.0.1",
]

[tool.uv]
package = false
